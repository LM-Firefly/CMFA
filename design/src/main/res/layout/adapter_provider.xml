<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <data>
        <variable
            name="provider"
            type="com.github.kr328.clash.core.model.Provider" />
        <variable
            name="state"
            type="com.github.kr328.clash.design.model.ProviderState" />
        <variable
            name="currentTime"
            type="com.github.kr328.clash.design.ui.ObservableCurrentTime" />
        <variable
            name="update"
            type="android.view.View.OnClickListener" />

        <import type="com.github.kr328.clash.design.util.I18nKt" />
        <import type="com.github.kr328.clash.design.util.IntervalKt" />
        <import type="android.view.View" />
    </data>

    <com.google.android.material.card.MaterialCardView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginStart="@dimen/item_header_margin"
        android:layout_marginEnd="@dimen/item_header_margin"
        android:layout_marginTop="4dp"
        android:layout_marginBottom="4dp"
        app:cardCornerRadius="12dp"
        app:cardElevation="2dp"
        app:contentPadding="16dp">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical">

            <!-- 标题行 -->
            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="horizontal"
                android:gravity="center_vertical">

                <TextView
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_weight="1"
                    android:text="@{provider.name}"
                    android:textAppearance="@style/TextAppearance.MaterialComponents.Body1"
                    android:textStyle="bold" />

                <FrameLayout
                    android:id="@+id/end_view"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:background="?attr/selectableItemBackground"
                    android:clickable="@{!state.updating}"
                    android:focusable="@{!state.updating}"
                    android:onClick="@{update}"
                    android:visibility="@{provider.vehicleType == com.github.kr328.clash.core.model.Provider.VehicleType.Inline ? View.GONE : View.VISIBLE}">

                    <ImageView
                        android:id="@+id/update_view"
                        android:layout_width="32dp"
                        android:layout_height="32dp"
                        android:layout_gravity="center"
                        android:padding="4dp"
                        android:contentDescription="@string/update"
                        android:src="@drawable/ic_baseline_swap_vert"
                        android:visibility="@{state.updating ? View.GONE : View.VISIBLE}" />

                    <ProgressBar
                        android:layout_width="24dp"
                        android:layout_height="24dp"
                        android:layout_gravity="center"
                        android:visibility="@{state.updating ? View.VISIBLE : View.GONE}" />
                </FrameLayout>
            </LinearLayout>

            <!-- 类型和时间信息 -->
            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="4dp"
                android:orientation="horizontal"
                android:gravity="center_vertical">

                <TextView
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_weight="1"
                    android:text="@{I18nKt.type(provider, context)}"
                    android:textAppearance="@style/TextAppearance.MaterialComponents.Caption"
                    android:textColor="?android:attr/textColorSecondary" />

                <TextView
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="@{IntervalKt.elapsedIntervalString(currentTime.value - state.updatedAt, context)}"
                    android:textAppearance="@style/TextAppearance.MaterialComponents.Caption"
                    android:textColor="?android:attr/textColorSecondary"
                    android:visibility="@{state.updatedAt > 0 ? View.VISIBLE : View.GONE}" />
                
                <TextView
                    android:id="@+id/percentage_text"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_marginStart="8dp"
                    android:text="@{I18nKt.getUsagePercentageText(provider)}"
                    android:textAppearance="@style/TextAppearance.MaterialComponents.Caption"
                    android:textColor="?android:attr/textColorSecondary"
                    android:visibility="@{I18nKt.hasSubscriptionInfo(provider) ? View.VISIBLE : View.GONE}" />
            </LinearLayout>

            <!-- 订阅信息 -->
            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="8dp"
                android:orientation="vertical"
                android:visibility="@{I18nKt.hasSubscriptionInfo(provider) ? View.VISIBLE : View.GONE}">

                <!-- 进度条 -->
                <ProgressBar
                    android:id="@+id/progress_bar"
                    style="?android:attr/progressBarStyleHorizontal"
                    android:layout_width="match_parent"
                    android:layout_height="8dp"
                    android:max="200"
                    android:progress="@{I18nKt.getUsagePercentage(provider)}" />

                <!-- 流量信息 -->
                <LinearLayout
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="4dp"
                    android:orientation="horizontal">

                    <TextView
                        android:layout_width="0dp"
                        android:layout_height="wrap_content"
                        android:layout_weight="1"
                        android:text="@{I18nKt.getRemainingTraffic(provider, context)}"
                        android:textAppearance="@style/TextAppearance.MaterialComponents.Caption"
                        android:textColor="?android:attr/textColorSecondary" />

                    <TextView
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:text="@{I18nKt.getUsedTraffic(provider, context)}"
                        android:textAppearance="@style/TextAppearance.MaterialComponents.Caption"
                        android:textColor="?android:attr/textColorSecondary" />
                </LinearLayout>

                <!-- 过期信息 -->
                <TextView
                    android:id="@+id/expire_text"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="4dp"
                    android:text="@{I18nKt.getExpireInfo(provider, context)}"
                    android:textAppearance="@style/TextAppearance.MaterialComponents.Caption"
                    android:textColor="?android:attr/textColorSecondary"
                    android:visibility="@{I18nKt.hasExpireInfo(provider) ? View.VISIBLE : View.GONE}" />
            </LinearLayout>
        </LinearLayout>
    </com.google.android.material.card.MaterialCardView>
</layout>